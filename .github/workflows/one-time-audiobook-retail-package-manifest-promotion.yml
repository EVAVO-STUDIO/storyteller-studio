name: One-time audiobook retail package-manifest promotion

on:
  push:
    branches:
      - main
    paths:
      - ".github/audiobook-retail-package-manifest.trigger"

permissions:
  contents: write
  statuses: write

jobs:
  promote:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Apply the isolated retail package-manifest slice by exact paths
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin work/audiobook-retail-package-manifest-20260728
          git checkout origin/work/audiobook-retail-package-manifest-20260728 -- \
            docs/AUDIOBOOK_RETAIL_PACKAGE_MANIFEST.md \
            package.json \
            packages/storyteller/package.json \
            packages/storyteller/src/audiobook-retail-package-manifest.test.ts \
            packages/storyteller/src/audiobook-retail-package-manifest.ts \
            scripts/check-audiobook-retail-package-manifest.mjs

          python3 - <<'PY' 2>&1 | tee .audiobook-retail-package-manifest-promotion.log
          from pathlib import Path
          import subprocess

          expected = {
              "docs/AUDIOBOOK_RETAIL_PACKAGE_MANIFEST.md",
              "package.json",
              "packages/storyteller/package.json",
              "packages/storyteller/src/audiobook-retail-package-manifest.test.ts",
              "packages/storyteller/src/audiobook-retail-package-manifest.ts",
              "scripts/check-audiobook-retail-package-manifest.mjs",
          }
          changed = {
              line.strip()
              for line in subprocess.check_output(
                  ["git", "diff", "--cached", "--name-only"],
                  text=True,
              ).splitlines()
              if line.strip()
          }
          print("AUDIOBOOK_RETAIL_PACKAGE_MANIFEST_FILES:" + ",".join(sorted(changed)))
          if changed != expected:
              raise SystemExit(
                  "AUDIOBOOK_RETAIL_PACKAGE_MANIFEST_FILE_SCOPE_INVALID:"
                  + ",".join(sorted(changed))
              )

          for temporary in [
              Path(
                  ".github/workflows/"
                  "one-time-audiobook-retail-package-manifest-promotion.yml"
              ),
              Path(".github/audiobook-retail-package-manifest.trigger"),
          ]:
              temporary.unlink(missing_ok=True)

          print("AUDIOBOOK_RETAIL_PACKAGE_MANIFEST_PROMOTION_APPLIED")
          PY

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Verify and build
        shell: bash
        run: |
          set -euo pipefail
          npm run verify 2>&1 | tee .audiobook-retail-package-manifest-verify.log
          npm run build 2>&1 | tee .audiobook-retail-package-manifest-build.log

      - name: Retain diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: audiobook-retail-package-manifest-${{ github.sha }}
          path: |
            .audiobook-retail-package-manifest-promotion.log
            .audiobook-retail-package-manifest-verify.log
            .audiobook-retail-package-manifest-build.log
          include-hidden-files: true
          if-no-files-found: warn
          retention-days: 7

      - name: Commit promotion
        shell: bash
        run: |
          set -euo pipefail
          rm -f \
            .audiobook-retail-package-manifest-promotion.log \
            .audiobook-retail-package-manifest-verify.log \
            .audiobook-retail-package-manifest-build.log
          git config user.name "EVAVO Repository Automation"
          git config user.email "174633320+EVAVO-STUDIO@users.noreply.github.com"
          git add --all
          git diff --cached --check
          git commit -m "Add governed audiobook retail package manifest"
          git pull --rebase origin main
          npm run verify
          npm run build
          git push origin HEAD:main

      - name: Publish status
        if: always()
        uses: actions/github-script@v7
        env:
          JOB_STATUS: ${{ job.status }}
        with:
          script: |
            const fs = require("node:fs");
            const state = process.env.JOB_STATUS === "success" ? "success" : "failure";
            const source = [
              ".audiobook-retail-package-manifest-promotion.log",
              ".audiobook-retail-package-manifest-verify.log",
              ".audiobook-retail-package-manifest-build.log",
            ].filter((path) => fs.existsSync(path)).map((path) => fs.readFileSync(path, "utf8")).join("\n").replace(/\u001b\[[0-9;]*m/g, "");
            const lines = source.split(/\r?\n/).map((line) => line.trim()).filter(Boolean);
            const diagnostic = lines.find((line) => /error TS\d+|TEST_FAILURE_DIAGNOSTIC|not ok|AssertionError|ERR_[A-Z_]+|check failed/i.test(line)) || lines.find((line) => /AUDIOBOOK_RETAIL_PACKAGE_MANIFEST_[A-Z0-9_]+/i.test(line)) || lines.at(-1) || "audiobook retail package-manifest promotion failed";
            const targetUrl = `${process.env.GITHUB_SERVER_URL}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state,
              context: "storyteller/audiobook-retail-package-manifest",
              description: state === "success" ? "Retail package-manifest tests, contracts and builds passed" : diagnostic.slice(0, 140),
              target_url: targetUrl,
            });
            if (state === "failure") {
              const slug = diagnostic.replace(/[^A-Za-z0-9._/-]+/g, "-").replace(/-+/g, "-").replace(/^[-./]+|[-./]+$/g, "").slice(0, 62) || "unknown";
              await github.rest.repos.createCommitStatus({
                owner: context.repo.owner,
                repo: context.repo.repo,
                sha: context.sha,
                state: "failure",
                context: `storyteller/failure/${slug}`,
                description: diagnostic.slice(0, 140),
                target_url: targetUrl,
              });
            }
