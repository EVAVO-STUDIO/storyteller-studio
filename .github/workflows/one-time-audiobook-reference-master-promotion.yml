name: One-time audiobook reference master promotion

on:
  push:
    branches:
      - main
    paths:
      - ".github/audiobook-reference-master-promotion.trigger"

permissions:
  contents: write
  statuses: write

jobs:
  promote:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Add governed reference-master tests, documentation and contracts
        shell: bash
        run: |
          set -o pipefail

          for path in \
            packages/storyteller/src/audiobook-reference-master.test.ts \
            scripts/check-audiobook-reference-master.mjs \
            docs/AUDIOBOOK_REFERENCE_MASTER.md
          do
            if [ -e "$path" ]; then
              echo "AUDIOBOOK_REFERENCE_MASTER_TARGET_EXISTS:$path"
              exit 1
            fi
          done

          cat > packages/storyteller/src/audiobook-reference-master.test.ts <<'TS'
          import assert from "node:assert/strict";
          import { mkdtemp, rm } from "node:fs/promises";
          import { tmpdir } from "node:os";
          import { join } from "node:path";
          import test from "node:test";
          import {
            assertAudiobookReferenceMasterChain,
            audiobookReferenceMasterPublicView,
            ingestAudiobookReferenceMaster,
            type AudiobookReferenceMasterChain,
          } from "./audiobook-reference-master.js";
          import {
            renderAudiobookSequence,
            type AudiobookRenderResult,
            type AudiobookSourceResolver,
            type ResolvedAudiobookSource,
          } from "./audiobook-render.js";
          import type {
            AudiobookSequence,
            AudiobookSequenceArtifactSnapshot,
            AudiobookSequenceComponent,
          } from "./audiobook-sequence.js";
          import type {
            ArtifactRecord,
            ArtifactRightsSnapshot,
          } from "./artifact-registry.js";
          import { FileArtifactRegistry } from "./artifact-store.js";
          import type {
            AudioEngineeringCommand,
            AudioEngineeringCommandResult,
            AudioEngineeringRunner,
          } from "./audio-engineering.js";
          import type {
            ChapterRenderRequest,
            ChapterRenderRunner,
          } from "./chapter-render.js";
          import {
            createGenerationAudioEngineeringPolicy,
            type GenerationAudioEngineeringPolicy,
          } from "./generation-audio-engineering.js";
          import {
            ACX_AUDIOBOOK_PROFILE,
            stableHash,
          } from "./index.js";
          import { FilePrivateObjectStore } from "./private-object-store.js";
          import { FileProjectStore } from "./project-store.js";

          const t0 = new Date("2026-07-28T00:00:00.000Z");
          const t1 = new Date("2026-07-28T00:00:01.000Z");
          const t2 = new Date("2026-07-28T00:00:02.000Z");
          const output = Object.freeze({
            format: "wav" as const,
            sampleRateHz: 44_100,
            channels: 1 as const,
            bitDepth: 24 as const,
          });

          function wavBytes(seed = 1): Uint8Array {
            return new Uint8Array([
              0x52, 0x49, 0x46, 0x46,
              0x08, 0x00, 0x00, 0x00,
              0x57, 0x41, 0x56, 0x45,
              seed, 0x01, 0x02, 0x03,
            ]);
          }

          function commandResult(
            stdout = "",
            stderr = "",
          ): AudioEngineeringCommandResult {
            return { exitCode: 0, stdout, stderr, durationMs: 5 };
          }

          class ReferenceEngineeringRunner implements AudioEngineeringRunner {
            constructor(
              readonly durationSeconds = 71,
              readonly loud = false,
              readonly failing = false,
            ) {}

            async run(
              command: AudioEngineeringCommand,
            ): Promise<AudioEngineeringCommandResult> {
              if (this.failing) {
                throw new Error("private engineering diagnostic must not escape");
              }
              switch (command.stage) {
                case "ffprobe-version":
                  return commandResult("ffprobe version 7.1\n");
                case "ffmpeg-version":
                  return commandResult("ffmpeg version 7.1\n");
                case "probe":
                  return commandResult(JSON.stringify({
                    streams: [{
                      codec_type: "audio",
                      codec_name: "pcm_s24le",
                      sample_rate: "44100",
                      channels: 1,
                      bit_rate: "192000",
                      duration: String(this.durationSeconds),
                    }],
                    format: {
                      format_name: "wav",
                      duration: String(this.durationSeconds),
                      bit_rate: "192000",
                      size: String(wavBytes().byteLength),
                    },
                  }));
                case "astats":
                  return commandResult([
                    `lavfi.astats.Overall.RMS_level=${this.loud ? -14 : -20}`,
                    `lavfi.astats.Overall.Peak_level=${this.loud ? 0 : -4}`,
                    `lavfi.astats.Overall.Noise_floor=${this.loud ? -45 : -65}`,
                    `lavfi.astats.Overall.Peak_count=${this.loud ? 12 : 0}`,
                  ].join("\n"));
                case "loudnorm":
                  return commandResult("", JSON.stringify({
                    input_i: this.loud ? "-14" : "-20",
                    input_tp: this.loud ? "0.2" : "-4.2",
                    input_lra: "4",
                    input_thresh: "-30",
                    target_offset: "0",
                  }));
                case "silence":
                  return commandResult("", [
                    "silence_start: 0",
                    "silence_end: 1 | silence_duration: 1",
                    `silence_start: ${this.durationSeconds - 1}`,
                    `silence_end: ${this.durationSeconds} | silence_duration: 1`,
                  ].join("\n"));
              }
            }
          }

          class ReferenceSourceResolver implements AudiobookSourceResolver {
            readonly disposed: string[] = [];

            async resolve(
              snapshot: AudiobookSequenceArtifactSnapshot,
            ): Promise<ResolvedAudiobookSource> {
              return {
                artifactId: snapshot.id,
                privatePath: `/private/storyteller/${snapshot.id}.wav`,
                contentHash: snapshot.contentHash,
                byteCount: snapshot.byteCount,
                dispose: async () => {
                  this.disposed.push(snapshot.id);
                },
              };
            }
          }

          class ReferenceRenderRunner implements ChapterRenderRunner {
            request?: ChapterRenderRequest;

            constructor(readonly bytes: Uint8Array = wavBytes()) {}

            async inspectVersion(): Promise<string> {
              return "ffmpeg version 7.1 fixture";
            }

            async render(request: ChapterRenderRequest): Promise<Uint8Array> {
              this.request = request;
              return this.bytes;
            }
          }

          function artifact(
            id: string,
            kind: "credit-master" | "mastered-chapter",
            fingerprintCharacter: string,
            contentCharacter: string,
          ): AudiobookSequenceArtifactSnapshot {
            return Object.freeze({
              id,
              kind,
              revision: 3,
              fingerprint: fingerprintCharacter.repeat(64),
              contentHash: contentCharacter.repeat(64),
              byteCount: 240_000 + Number.parseInt(fingerprintCharacter, 16),
            });
          }

          function component(input: Readonly<{
            ordinal: number;
            role: AudiobookSequenceComponent["role"];
            title: string;
            durationMs: number;
            startMs: number;
            artifact: AudiobookSequenceArtifactSnapshot;
            sourceFingerprint: string;
          }>): AudiobookSequenceComponent {
            const partial: Omit<AudiobookSequenceComponent, "fingerprint"> = {
              ordinal: input.ordinal,
              role: input.role,
              title: input.title,
              durationMs: input.durationMs,
              startMs: input.startMs,
              endMs: input.startMs + input.durationMs,
              artifact: input.artifact,
              sourceFingerprint: input.sourceFingerprint,
            };
            return Object.freeze({
              ...partial,
              fingerprint: stableHash(partial),
            });
          }

          function sequence(
            engineeringProfileFingerprint: string,
          ): AudiobookSequence {
            const opening = component({
              ordinal: 1,
              role: "opening-credit",
              title: "Opening credit",
              durationMs: 5_000,
              startMs: 0,
              artifact: artifact(
                "artifact_reference_opening_001",
                "credit-master",
                "1",
                "4",
              ),
              sourceFingerprint: "7".repeat(64),
            });
            const chapter = component({
              ordinal: 2,
              role: "chapter",
              title: "Chapter One",
              durationMs: 60_000,
              startMs: opening.endMs,
              artifact: artifact(
                "artifact_reference_chapter_001",
                "mastered-chapter",
                "2",
                "5",
              ),
              sourceFingerprint: "8".repeat(64),
            });
            const closing = component({
              ordinal: 3,
              role: "closing-credit",
              title: "Closing credit",
              durationMs: 6_000,
              startMs: chapter.endMs,
              artifact: artifact(
                "artifact_reference_closing_001",
                "credit-master",
                "3",
                "6",
              ),
              sourceFingerprint: "9".repeat(64),
            });
            const partial: Omit<AudiobookSequence, "fingerprint"> = {
              schemaVersion: "storyteller-audiobook-sequence-v1",
              id: "audiobook_reference_sequence_001",
              projectId: "project_audiobook_reference_001",
              bookId: "book_audiobook_reference_001",
              title: "Reference Master Fixture",
              languageTag: "en-AU",
              chapterSequenceFingerprint: "b".repeat(64),
              openingDeliveryFingerprint: "c".repeat(64),
              closingDeliveryFingerprint: "d".repeat(64),
              rightsFingerprint: "a".repeat(64),
              engineeringProfileFingerprint,
              output,
              components: Object.freeze([opening, chapter, closing]),
              chapterCount: 1,
              totalDurationMs: closing.endMs,
              status: "ready-for-retail-encoding",
              createdByActorId: "owner_audiobook_reference_001",
              revision: 1,
              createdAt: t0.toISOString(),
              updatedAt: t0.toISOString(),
            };
            return Object.freeze({
              ...partial,
              fingerprint: stableHash(partial),
            });
          }

          function policy(
            temporaryRoot: string,
            runner: AudioEngineeringRunner,
          ): GenerationAudioEngineeringPolicy {
            return createGenerationAudioEngineeringPolicy({
              profile: ACX_AUDIOBOOK_PROFILE,
              externalVersion: "acx-2026-07",
              reviewedAt: "2026-07-27T00:00:00.000Z",
              sourceReference: "acx-audio-submission-requirements-reviewed-2026-07",
              runner,
              ffprobePath: "/opt/media/ffprobe",
              ffmpegPath: "/opt/media/ffmpeg",
              timeoutMs: 30_000,
              maximumOutputBytes: 2 * 1024 * 1024,
              temporaryRoot,
              now: t0,
            });
          }

          function rights(
            overrides: Partial<ArtifactRightsSnapshot> = {},
          ): ArtifactRightsSnapshot {
            return {
              rightsEvidenceId: "rights_audiobook_reference_001",
              rightsFingerprint: "a".repeat(64),
              allowedUses: ["audiobook"],
              commercialUseApproved: true,
              expiresAt: "2028-07-28T00:00:00.000Z",
              retainUntil: "2033-07-28T00:00:00.000Z",
              deletionRequiredAt: "2034-07-28T00:00:00.000Z",
              ...overrides,
            };
          }

          function withSequenceFingerprint(
            value: AudiobookSequence,
            updates: Partial<Omit<AudiobookSequence, "fingerprint">>,
          ): AudiobookSequence {
            const { fingerprint: _fingerprint, ...base } = value;
            const partial: Omit<AudiobookSequence, "fingerprint"> = {
              ...base,
              ...updates,
            };
            return Object.freeze({
              ...partial,
              fingerprint: stableHash(partial),
            });
          }

          async function renderFixture(
            value: AudiobookSequence,
            bytes = wavBytes(),
          ): Promise<AudiobookRenderResult> {
            return await renderAudiobookSequence({
              sequence: value,
              sources: new ReferenceSourceResolver(),
              runner: new ReferenceRenderRunner(bytes),
              renderedAt: t1,
              maximumOutputBytes: 100_000_000,
            });
          }

          async function withStores(
            run: (input: Readonly<{
              root: string;
              temporaryRoot: string;
              objectStore: FilePrivateObjectStore;
              registry: FileArtifactRegistry;
            }>) => Promise<void>,
          ): Promise<void> {
            const root = await mkdtemp(
              join(tmpdir(), "storyteller-audiobook-reference-master-"),
            );
            try {
              const temporaryRoot = join(root, "temporary");
              await run({
                root,
                temporaryRoot,
                objectStore: new FilePrivateObjectStore(join(root, "objects")),
                registry: new FileArtifactRegistry(
                  new FileProjectStore(join(root, "registry")),
                ),
              });
            } finally {
              await rm(root, { recursive: true, force: true });
            }
          }

          test("eligible complete-book render creates a governed reference master and evidence graph", async () => {
            await withStores(async ({
              temporaryRoot,
              objectStore,
              registry,
            }) => {
              const engineering = policy(
                temporaryRoot,
                new ReferenceEngineeringRunner(),
              );
              const plan = sequence(engineering.profile.fingerprint);
              const render = await renderFixture(plan);
              const chain = await ingestAudiobookReferenceMaster(
                objectStore,
                registry,
                {
                  sequence: plan,
                  render,
                  rights: rights(),
                  actorId: "worker_audiobook_reference_001",
                  verifierActorId: "verifier_audiobook_reference_001",
                  engineering,
                  now: t2,
                },
              );

              assert.equal(chain.eligibleForReview, true);
              assert.deepEqual(chain.findingCodes, []);
              assert.equal(
                chain.sequenceManifest.payload.kind,
                "audio-analysis",
              );
              assert.equal(chain.renderEvidence.payload.kind, "audio-analysis");
              assert.equal(
                chain.referenceMaster.payload.kind,
                "audiobook-reference-master",
              );
              assert.equal(
                chain.postRenderEngineering.ingest.envelope.payload.kind,
                "audio-analysis",
              );
              assert.equal(
                chain.referenceMaster.payload.verification.status,
                "verified",
              );
              assert.equal(chain.referenceMaster.payload.review.status, "pending");
              assert.deepEqual(
                chain.referenceMaster.payload.provenance.parentArtifactIds,
                [
                  chain.sequenceManifest.payload.id,
                  chain.renderEvidence.payload.id,
                ],
              );
              assert.deepEqual(
                chain.postRenderEngineering.ingest.envelope.payload.provenance
                  .parentArtifactIds,
                [chain.referenceMaster.payload.id],
              );
              assert.equal(
                chain.postRenderEngineering.evidence.inputContentHash,
                chain.referenceMaster.payload.integrity.contentHash,
              );
              assert.equal(
                chain.postRenderEngineering.evidence.inputByteCount,
                chain.referenceMaster.payload.integrity.byteCount,
              );
              assert.equal((await registry.list()).length, 4);
              assert.doesNotThrow(() =>
                assertAudiobookReferenceMasterChain(chain)
              );

              const view = audiobookReferenceMasterPublicView(chain);
              const serialised = JSON.stringify(view);
              assert.equal(view.eligibleForReview, true);
              for (const forbidden of [
                temporaryRoot,
                "/opt/media/ffprobe",
                "/opt/media/ffmpeg",
                "acx-audio-submission-requirements-reviewed-2026-07",
                "rights_audiobook_reference_001",
                "worker_audiobook_reference_001",
                "verifier_audiobook_reference_001",
                chain.sequenceManifest.payload.id,
                chain.renderEvidence.payload.id,
                ...plan.components.map((entry) => entry.artifact.id),
              ]) {
                assert.equal(serialised.includes(forbidden), false);
              }
            });
          });

          test("independent engineering failure quarantines the WAV while retaining evidence", async () => {
            await withStores(async ({
              temporaryRoot,
              objectStore,
              registry,
            }) => {
              const engineering = policy(
                temporaryRoot,
                new ReferenceEngineeringRunner(71, true),
              );
              const plan = sequence(engineering.profile.fingerprint);
              const chain = await ingestAudiobookReferenceMaster(
                objectStore,
                registry,
                {
                  sequence: plan,
                  render: await renderFixture(plan),
                  rights: rights(),
                  actorId: "worker_audiobook_reference_002",
                  verifierActorId: "verifier_audiobook_reference_002",
                  engineering,
                  now: t2,
                },
              );

              assert.equal(chain.eligibleForReview, false);
              assert.equal(
                chain.referenceMaster.payload.verification.status,
                "quarantined",
              );
              assert.equal(
                chain.referenceMaster.payload.quarantine?.code,
                "AUDIOBOOK_REFERENCE_MASTER_ENGINEERING_INELIGIBLE",
              );
              for (const code of [
                "AUDIO_RMS_OUT_OF_RANGE",
                "AUDIO_PEAK_TOO_HIGH",
                "AUDIO_NOISE_FLOOR_TOO_HIGH",
                "AUDIO_CLIPPING_DETECTED",
              ]) {
                assert.equal(chain.findingCodes.includes(code), true);
              }
              assert.equal(
                chain.postRenderEngineering.ingest.envelope.payload
                  .verification.status,
                "verified",
              );
              assert.equal((await registry.list()).length, 4);
              assert.doesNotThrow(() =>
                assertAudiobookReferenceMasterChain(chain)
              );
            });
          });

          test("duration drift is a separate complete-book quarantine gate", async () => {
            await withStores(async ({
              temporaryRoot,
              objectStore,
              registry,
            }) => {
              const engineering = policy(
                temporaryRoot,
                new ReferenceEngineeringRunner(72),
              );
              const plan = sequence(engineering.profile.fingerprint);
              const chain = await ingestAudiobookReferenceMaster(
                objectStore,
                registry,
                {
                  sequence: plan,
                  render: await renderFixture(plan),
                  rights: rights(),
                  actorId: "worker_audiobook_reference_003",
                  verifierActorId: "verifier_audiobook_reference_003",
                  engineering,
                  maximumDurationDriftMs: 250,
                  now: t2,
                },
              );

              assert.equal(
                chain.postRenderEngineering.candidateEligible,
                true,
              );
              assert.equal(chain.durationDriftMs, 1_000);
              assert.equal(chain.eligibleForReview, false);
              assert.equal(
                chain.findingCodes.includes(
                  "AUDIOBOOK_REFERENCE_MASTER_DURATION_DRIFT",
                ),
                true,
              );
              assert.equal(
                chain.referenceMaster.payload.verification.status,
                "quarantined",
              );
            });
          });

          test("identical retries reuse the exact four-artifact reference chain", async () => {
            await withStores(async ({
              temporaryRoot,
              objectStore,
              registry,
            }) => {
              const engineering = policy(
                temporaryRoot,
                new ReferenceEngineeringRunner(),
              );
              const plan = sequence(engineering.profile.fingerprint);
              const render = await renderFixture(plan);
              const input = {
                sequence: plan,
                render,
                rights: rights(),
                actorId: "worker_audiobook_reference_004",
                verifierActorId: "verifier_audiobook_reference_004",
                engineering,
                now: t2,
              } as const;

              const first = await ingestAudiobookReferenceMaster(
                objectStore,
                registry,
                input,
              );
              const second = await ingestAudiobookReferenceMaster(
                objectStore,
                registry,
                input,
              );

              assert.equal(second.fingerprint, first.fingerprint);
              assert.equal(
                second.referenceMaster.payload.id,
                first.referenceMaster.payload.id,
              );
              assert.equal(
                second.referenceMaster.revision,
                first.referenceMaster.revision,
              );
              assert.equal(
                second.postRenderEngineering.ingest.envelope.revision,
                first.postRenderEngineering.ingest.envelope.revision,
              );
              assert.equal((await registry.list()).length, 4);
            });
          });

          test("scope, rights, bytes, tolerance and abort failures occur before artifact admission", async () => {
            await withStores(async ({
              temporaryRoot,
              objectStore,
              registry,
            }) => {
              const engineering = policy(
                temporaryRoot,
                new ReferenceEngineeringRunner(),
              );
              const plan = sequence(engineering.profile.fingerprint);
              const render = await renderFixture(plan);
              const wrongProfile = withSequenceFingerprint(plan, {
                engineeringProfileFingerprint: "f".repeat(64),
              });

              await assert.rejects(
                ingestAudiobookReferenceMaster(objectStore, registry, {
                  sequence: wrongProfile,
                  render: await renderFixture(wrongProfile),
                  rights: rights(),
                  actorId: "worker_audiobook_reference_005",
                  engineering,
                  now: t2,
                }),
                /AUDIOBOOK_REFERENCE_MASTER_ENGINEERING_PROFILE_MISMATCH/u,
              );
              await assert.rejects(
                ingestAudiobookReferenceMaster(objectStore, registry, {
                  sequence: plan,
                  render,
                  rights: rights({
                    expiresAt: "2026-07-27T00:00:00.000Z",
                  }),
                  actorId: "worker_audiobook_reference_005",
                  engineering,
                  now: t2,
                }),
                /AUDIOBOOK_REFERENCE_MASTER_RIGHTS_EXPIRED/u,
              );
              const changedBytes = new Uint8Array(render.bytes);
              changedBytes[changedBytes.length - 1] ^= 0xff;
              await assert.rejects(
                ingestAudiobookReferenceMaster(objectStore, registry, {
                  sequence: plan,
                  render: { evidence: render.evidence, bytes: changedBytes },
                  rights: rights(),
                  actorId: "worker_audiobook_reference_005",
                  engineering,
                  now: t2,
                }),
                /AUDIOBOOK_REFERENCE_MASTER_RENDER_OUTPUT_MISMATCH/u,
              );
              await assert.rejects(
                ingestAudiobookReferenceMaster(objectStore, registry, {
                  sequence: plan,
                  render,
                  rights: rights(),
                  actorId: "worker_audiobook_reference_005",
                  engineering,
                  maximumDurationDriftMs: 10_001,
                  now: t2,
                }),
                /AUDIOBOOK_REFERENCE_MASTER_DURATION_TOLERANCE_INVALID/u,
              );
              const controller = new AbortController();
              controller.abort();
              await assert.rejects(
                ingestAudiobookReferenceMaster(objectStore, registry, {
                  sequence: plan,
                  render,
                  rights: rights(),
                  actorId: "worker_audiobook_reference_005",
                  engineering,
                  now: t2,
                  signal: controller.signal,
                }),
                /AUDIOBOOK_REFERENCE_MASTER_ABORTED/u,
              );
              assert.equal((await registry.list()).length, 0);
            });
          });

          test("semantic artifact-chain tampering is rejected even when hashes are recomputed", async () => {
            await withStores(async ({
              temporaryRoot,
              objectStore,
              registry,
            }) => {
              const engineering = policy(
                temporaryRoot,
                new ReferenceEngineeringRunner(),
              );
              const plan = sequence(engineering.profile.fingerprint);
              const chain = await ingestAudiobookReferenceMaster(
                objectStore,
                registry,
                {
                  sequence: plan,
                  render: await renderFixture(plan),
                  rights: rights(),
                  actorId: "worker_audiobook_reference_006",
                  verifierActorId: "verifier_audiobook_reference_006",
                  engineering,
                  now: t2,
                },
              );

              const original = chain.referenceMaster.payload;
              const { fingerprint: _fingerprint, ...recordBase } = original;
              const alteredRecordBase = {
                ...recordBase,
                provenance: {
                  ...original.provenance,
                  parentArtifactIds: Object.freeze([
                    chain.sequenceManifest.payload.id,
                  ]),
                },
              };
              const alteredRecord: ArtifactRecord = {
                ...alteredRecordBase,
                fingerprint: stableHash(alteredRecordBase),
              };
              const alteredEnvelope = {
                ...chain.referenceMaster,
                payload: alteredRecord,
                contentHash: stableHash(alteredRecord),
              };
              const altered = {
                ...chain,
                referenceMaster: alteredEnvelope,
              } as AudiobookReferenceMasterChain;

              assert.throws(
                () => assertAudiobookReferenceMasterChain(altered),
                /AUDIOBOOK_REFERENCE_MASTER_EVIDENCE_CHAIN_MISMATCH/u,
              );
            });
          });
          TS

          sed -i 's/^          //' packages/storyteller/src/audiobook-reference-master.test.ts

          cat > scripts/check-audiobook-reference-master.mjs <<'MJS'
          import { existsSync, readFileSync, readdirSync, statSync } from "node:fs";
          import { join, relative, resolve } from "node:path";

          const root = process.cwd();
          const problems = [];
          const fromRoot = (path) => resolve(root, path);
          const read = (path) => readFileSync(fromRoot(path), "utf8");

          function requireFile(path) {
            if (!existsSync(fromRoot(path))) {
              problems.push(`missing audiobook-reference-master file: ${path}`);
            }
          }

          function requireTokens(path, tokens) {
            if (!existsSync(fromRoot(path))) return;
            const source = read(path);
            for (const token of tokens) {
              if (!source.includes(token)) {
                problems.push(
                  `${path} is missing audiobook-reference-master contract token: ${token}`,
                );
              }
            }
          }

          function collectRuntimeFiles(directory, output = []) {
            const absolute = fromRoot(directory);
            if (!existsSync(absolute)) return output;
            for (const name of readdirSync(absolute)) {
              const absolutePath = join(absolute, name);
              const item = statSync(absolutePath);
              if (item.isDirectory()) {
                collectRuntimeFiles(relative(root, absolutePath), output);
              } else if (
                /\.(?:ts|tsx|js|mjs)$/u.test(name)
                && !/\.(?:test|spec)\.[^.]+$/u.test(name)
              ) {
                output.push(relative(root, absolutePath).replaceAll("\\", "/"));
              }
            }
            return output;
          }

          for (const path of [
            "packages/storyteller/src/audiobook-reference-master.ts",
            "packages/storyteller/src/audiobook-reference-master.test.ts",
            "packages/storyteller/package.json",
            "docs/AUDIOBOOK_REFERENCE_MASTER.md",
          ]) requireFile(path);

          requireTokens("packages/storyteller/src/audiobook-reference-master.ts", [
            "AUDIOBOOK_REFERENCE_MASTER_SCHEMA_VERSION",
            "ingestAudiobookReferenceMaster",
            "assertAudiobookReferenceMasterChain",
            "audiobookReferenceMasterPublicView",
            'kind: "audiobook-reference-master"',
            "audiobook-sequence-schema",
            "audiobook-render-evidence-schema",
            "ingestAudioEngineeringArtifact",
            "assertEvidenceMatchesGenerationPolicy",
            "AUDIOBOOK_REFERENCE_MASTER_ENGINEERING_INELIGIBLE",
            "AUDIOBOOK_REFERENCE_MASTER_DURATION_DRIFT",
            "AUDIOBOOK_REFERENCE_MASTER_EVIDENCE_CHAIN_MISMATCH",
            "AUDIOBOOK_REFERENCE_MASTER_FINGERPRINT_INVALID",
          ]);

          requireTokens("packages/storyteller/src/audiobook-reference-master.test.ts", [
            "eligible complete-book render creates a governed reference master and evidence graph",
            "independent engineering failure quarantines the WAV while retaining evidence",
            "duration drift is a separate complete-book quarantine gate",
            "identical retries reuse the exact four-artifact reference chain",
            "scope, rights, bytes, tolerance and abort failures occur before artifact admission",
            "semantic artifact-chain tampering is rejected even when hashes are recomputed",
            "AUDIOBOOK_REFERENCE_MASTER_EVIDENCE_CHAIN_MISMATCH",
          ]);

          requireTokens("docs/AUDIOBOOK_REFERENCE_MASTER.md", [
            "Evidence graph",
            "Independent engineering",
            "Quarantine rather than overwrite",
            "Human review boundary",
            "Idempotency",
            "Privacy boundary",
            "Current boundary",
            "not a released retail audiobook",
          ]);

          if (existsSync(fromRoot("packages/storyteller/package.json"))) {
            const packageJson = JSON.parse(read("packages/storyteller/package.json"));
            if (
              packageJson.exports?.["./audiobook-reference-master"]
                !== "./src/audiobook-reference-master.ts"
            ) {
              problems.push(
                "storyteller package does not export ./audiobook-reference-master",
              );
            }
          }

          if (existsSync(fromRoot("package.json"))) {
            const packageJson = JSON.parse(read("package.json"));
            if (
              packageJson.scripts?.["verify:audiobook-reference-master"]
                !== "node scripts/check-audiobook-reference-master.mjs"
            ) {
              problems.push(
                "root package does not expose verify:audiobook-reference-master",
              );
            }
            if (
              !packageJson.scripts?.["verify:artifacts"]?.includes(
                "npm run verify:audiobook-reference-master",
              )
            ) {
              problems.push(
                "permanent artifact verification omits audiobook reference masters",
              );
            }
          }

          const source = existsSync(
            fromRoot("packages/storyteller/src/audiobook-reference-master.ts"),
          )
            ? read("packages/storyteller/src/audiobook-reference-master.ts")
            : "";
          const publicStart = source.indexOf(
            "export function audiobookReferenceMasterPublicView",
          );
          if (publicStart < 0) {
            problems.push("audiobook reference-master public view is missing");
          } else {
            const publicSource = source.slice(publicStart);
            for (const forbidden of [
              "objectKey",
              "container",
              "versionId",
              "rightsEvidenceId",
              "actorId",
              "verifierActorId",
              "parentArtifactIds",
              "sourceReference",
              "commandFingerprints",
            ]) {
              if (publicSource.includes(forbidden)) {
                problems.push(
                  `audiobook reference-master public view exposes private state: ${forbidden}`,
                );
              }
            }
          }

          for (const path of [
            ...collectRuntimeFiles("apps/api/src"),
            ...collectRuntimeFiles("apps/web/src"),
          ]) {
            const runtime = read(path);
            for (const forbidden of [
              "@evavo/storyteller-engine/audiobook-reference-master",
              "ingestAudiobookReferenceMaster",
            ]) {
              if (runtime.includes(forbidden)) {
                problems.push(
                  `${path} exposes private reference-master controls: ${forbidden}`,
                );
              }
            }
          }

          if (problems.length > 0) {
            console.error(
              "Storyteller Studio audiobook-reference-master check failed:\n",
            );
            for (const problem of problems) console.error(`- ${problem}`);
            process.exit(1);
          }

          console.log("storyteller_audiobook_reference_master_check_passed");
          console.log("- complete-book WAV bytes remain private and content-addressed");
          console.log("- sequence, render and engineering evidence form one immutable graph");
          console.log("- engineering or duration failure quarantines rather than overwrites");
          console.log("- retry identity remains stable across the four-artifact chain");
          console.log("- human review remains mandatory before retail encoding or release");
          MJS

          sed -i 's/^          //' scripts/check-audiobook-reference-master.mjs

          cat > docs/AUDIOBOOK_REFERENCE_MASTER.md <<'MD'
          # Governed audiobook reference master

          The reference-master boundary turns an approved complete-audiobook sequence and its shell-free render evidence into a private, independently measured WAV that can enter whole-book listening review.

          It does not treat a successful FFmpeg process as an approved book. Rendering, storage verification, independent engineering, human listening review, retail encoding and release remain separate decisions.

          ## Evidence graph

          The governed graph is:

          ```text
          approved audiobook sequence
            -> immutable sequence-manifest artifact
            -> immutable render-evidence artifact
            -> private audiobook-reference-master WAV
            -> independent post-render audio-analysis artifact
          ```

          Every node is project-, job-, segment- and take-scoped. The reference WAV directly parents the approved sequence and render evidence. The engineering artifact directly parents the exact stored WAV and records the same content hash and byte count.

          ## Admission

          Ingestion revalidates the complete sequence, exact component order, render source snapshots, rendered-byte hash and size, output format, sample rate, channel count, bit depth, rights fingerprint and reviewed engineering-profile fingerprint before artifact admission.

          The complete-book bytes must be WAV and remain in private content-addressed storage. A reference master cannot be represented by a transcript, JSON report, provider response or chapter artifact relabelled under a new name.

          ## Independent engineering

          Post-render analysis runs through the existing bounded FFprobe and FFmpeg engineering boundary. The resulting evidence retains tool versions, command fingerprints, probe observations, technical measurements, silence observations, reviewed profile evidence and safe finding codes.

          The reference-master comparison also checks expected versus observed complete-book duration, sample rate, channel layout and PCM bit depth. These checks are independent of the renderer's own evidence.

          ## Quarantine rather than overwrite

          A technically ineligible render or a complete-book duration/profile mismatch does not replace or erase evidence. The WAV is quarantined, the analysis artifact remains verified and auditable, and the chain reports the exact safe finding codes.

          The approved component masters are never overwritten. Corrective work must create a new governed sequence or render intent rather than mutating the failed reference bytes.

          ## Human review boundary

          An eligible reference master begins with human review pending. Engineering eligibility only permits listening review; it does not approve performance continuity, opening and closing transitions, chapter order, pacing, credits, silence shape or sustained whole-book listenability.

          A later review domain must bind an explicit human decision to the exact reference artifact revision before retail encoding can begin.

          ## Idempotency

          Artifact identifiers derive from immutable sequence, render and content evidence. Retrying identical production intent reuses the same content-addressed bytes and the same four artifact envelopes. A changed sequence, render command, output hash or engineering evidence cannot silently redefine an existing identity.

          ## Privacy boundary

          The public projection contains safe status, profile, duration and fingerprint data. It omits private object keys, containers, version locators, executable paths, temporary paths, source component identifiers, parent artifact identifiers, rights evidence, actor identities and raw command details.

          Normal web and API runtimes do not receive the ingestion function. The boundary belongs to private production infrastructure and cannot be invoked from a public route.

          ## Current boundary

          This layer creates a governed whole-book listening reference. It is not a released retail audiobook and it is not yet a distributor package.

          Still required after reference-master ingestion:

          1. dedicated whole-book human listening review and revision history;
          2. approved encoding profiles for each distributor target;
          3. metadata, cover, chapter-marker and credit verification;
          4. encoded-file engineering and exact source-to-encode provenance;
          5. release-package assembly, checksum manifest and explicit final confirmation.
          MD

          sed -i 's/^          //' docs/AUDIOBOOK_REFERENCE_MASTER.md

          python3 - <<'PY' 2>&1 | tee .audiobook-reference-master-promotion.log
          import json
          from pathlib import Path

          package_path = Path("packages/storyteller/package.json")
          package = json.loads(package_path.read_text(encoding="utf-8"))
          exports = package.get("exports")
          if not isinstance(exports, dict):
              raise SystemExit("AUDIOBOOK_REFERENCE_MASTER_EXPORTS_INVALID")
          expected = "./src/audiobook-reference-master.ts"
          current = exports.get("./audiobook-reference-master")
          if current not in (None, expected):
              raise SystemExit(
                  f"AUDIOBOOK_REFERENCE_MASTER_EXPORT_CONFLICT:{current}"
              )
          if current is None:
              rebuilt = {}
              inserted = False
              for key, value in exports.items():
                  if key == "./audiobook-render":
                      rebuilt["./audiobook-reference-master"] = expected
                      inserted = True
                  rebuilt[key] = value
              if not inserted:
                  raise SystemExit("AUDIOBOOK_REFERENCE_MASTER_EXPORT_ANCHOR_MISSING")
              package["exports"] = rebuilt
          package_path.write_text(
              json.dumps(package, indent=2, ensure_ascii=False) + "\n",
              encoding="utf-8",
              newline="\n",
          )

          root_path = Path("package.json")
          root = json.loads(root_path.read_text(encoding="utf-8"))
          scripts = root.get("scripts")
          if not isinstance(scripts, dict):
              raise SystemExit("AUDIOBOOK_REFERENCE_MASTER_SCRIPTS_INVALID")
          check_command = "node scripts/check-audiobook-reference-master.mjs"
          current_check = scripts.get("verify:audiobook-reference-master")
          if current_check not in (None, check_command):
              raise SystemExit(
                  f"AUDIOBOOK_REFERENCE_MASTER_SCRIPT_CONFLICT:{current_check}"
              )
          scripts["verify:audiobook-reference-master"] = check_command
          verify_artifacts = scripts.get("verify:artifacts")
          if not isinstance(verify_artifacts, str):
              raise SystemExit("AUDIOBOOK_REFERENCE_MASTER_VERIFY_ARTIFACTS_INVALID")
          token = "npm run verify:audiobook-reference-master"
          if token not in verify_artifacts:
              anchor = "npm run verify:audiobook-render && npm run verify:mastering-render"
              replacement = (
                  "npm run verify:audiobook-render && "
                  "npm run verify:audiobook-reference-master && "
                  "npm run verify:mastering-render"
              )
              count = verify_artifacts.count(anchor)
              print(f"AUDIOBOOK_REFERENCE_MASTER_VERIFY_ANCHOR:{count}")
              if count != 1:
                  raise SystemExit(
                      f"AUDIOBOOK_REFERENCE_MASTER_VERIFY_ANCHOR_INVALID:{count}"
                  )
              scripts["verify:artifacts"] = verify_artifacts.replace(
                  anchor,
                  replacement,
                  1,
              )
          root_path.write_text(
              json.dumps(root, indent=2, ensure_ascii=False) + "\n",
              encoding="utf-8",
              newline="\n",
          )

          for temporary in [
              Path(".github/workflows/one-time-audiobook-reference-master-promotion.yml"),
              Path(".github/audiobook-reference-master-promotion.trigger"),
          ]:
              temporary.unlink(missing_ok=True)

          print("AUDIOBOOK_REFERENCE_MASTER_PROMOTION_APPLIED")
          PY

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Verify and build
        shell: bash
        run: |
          set -o pipefail
          npm run verify 2>&1 | tee .audiobook-reference-master-verify.log
          npm run build 2>&1 | tee .audiobook-reference-master-build.log

      - name: Retain diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: audiobook-reference-master-promotion-${{ github.sha }}
          path: |
            .audiobook-reference-master-promotion.log
            .audiobook-reference-master-verify.log
            .audiobook-reference-master-build.log
          include-hidden-files: true
          if-no-files-found: warn
          retention-days: 7

      - name: Commit promotion
        shell: bash
        run: |
          rm -f .audiobook-reference-master-promotion.log .audiobook-reference-master-verify.log .audiobook-reference-master-build.log
          git config user.name "EVAVO Repository Automation"
          git config user.email "174633320+EVAVO-STUDIO@users.noreply.github.com"
          git add --all
          git diff --cached --check
          git commit -m "Verify governed audiobook reference masters"
          git pull --rebase origin main
          npm run verify
          npm run build
          git push origin HEAD:main

      - name: Publish status
        if: always()
        uses: actions/github-script@v7
        env:
          JOB_STATUS: ${{ job.status }}
        with:
          script: |
            const fs = require("node:fs");
            const state = process.env.JOB_STATUS === "success" ? "success" : "failure";
            const source = [
              ".audiobook-reference-master-promotion.log",
              ".audiobook-reference-master-verify.log",
              ".audiobook-reference-master-build.log",
            ].filter((path) => fs.existsSync(path)).map((path) => fs.readFileSync(path, "utf8")).join("\n").replace(/\u001b\[[0-9;]*m/g, "");
            const lines = source.split(/\r?\n/).map((line) => line.trim()).filter(Boolean);
            const diagnostic = lines.find((line) => /AUDIOBOOK_REFERENCE_MASTER_|error TS\d+|TEST_FAILURE_DIAGNOSTIC|not ok|AssertionError|ERR_[A-Z_]+/i.test(line)) || lines.at(-1) || "audiobook reference master promotion failed";
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state,
              context: "storyteller/audiobook-reference-master-promotion",
              description: state === "success" ? "Reference-master tests, contracts and builds passed" : diagnostic.slice(0, 140),
              target_url: `${process.env.GITHUB_SERVER_URL}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
            });
